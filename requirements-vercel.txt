# ─── Vercel-only slim requirements ────────────────────────────────────────────
# Heavy ML packages (torch, sb3, gymnasium, optuna, hmmlearn, mlflow) are
# EXCLUDED here because:
#   1. Vercel's lambda size limit is 250 MB compressed.
#   2. The dashboard only needs Dash + data libs at runtime.
#   3. ML training is never triggered from the Vercel deployment.
# ──────────────────────────────────────────────────────────────────────────────

# Web / Dashboard
dash==2.17.0
dash-bootstrap-components==1.6.0
plotly==5.22.0
flask-caching==2.3.0
gunicorn==22.0.0

# Data
yfinance==0.2.40
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
polars==0.20.18
pyarrow==15.0.2
scikit-learn==1.5.0

# HTTP
httpx==0.27.0
